#include <stdio.h>

/**
 * _abs - prints the absolute valie of an integer
 * @n: argument to be checked
 *
 * Return: Actual digit
 */
int _abs(int n)
{
	if (int n < 0)
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}
}
