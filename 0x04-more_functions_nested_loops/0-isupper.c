#include "main.h"
#include <stdio.h>

/**
 * _isupper - check whether an input is uppercase or lower
 * @c: Checked input
 *
 * Return: 1 if upper 0 if otherwise
 */
int _isupper(int c)
{
	if (_isupper(c) == 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
